This component represents a container component for another component with its value
turned on or off (cleared and disabled) with a checkbox. Thus, it needs a component with "disabled" property as its child
