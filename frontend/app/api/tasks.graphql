# import DeletedFragment from "deleted.graphql"

fragment TaskFragment on GTask {
  ref
  status
  created
  nameLabel
  nameDescription
  finished
  progress
  result
  residentOn
  objectRef
  objectType
}

subscription Tasks{
  tasks {
    value{
      ...TaskFragment
      ...DeletedFragment
    }
    changeType
  }
}

query TaskList {
  tasks {
    ...TaskFragment
  }
}

query TaskInfo ($ref: ID!) {
  task(ref: $ref) {
    ...TaskFragment
  }
}
